-- 0004 분석 실행 내역
--
-- [분석 실행] 을 눌렀는데 추천이 0건이면 관리자는 "아무 일도 안 일어났다" 고 느낀다.
-- 실제로는 이유가 있다. 이미 검토한 주제라 다시 제안하지 않았거나, 표본이 적거나,
-- 기존 메뉴가 이미 답하고 있거나. 그 내역을 남겨서 화면이 설명할 수 있게 한다.
--
-- 컬럼을 여럿 만들지 않고 JSONB 하나로 둔다. 어떤 수치를 보여줄지는 아직 바뀔 수 있다.

ALTER TABLE analysis_run ADD COLUMN stats JSONB NOT NULL DEFAULT '{}';
